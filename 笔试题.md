# 量化研究员笔试题

> **考试时间**：48小时（take-home）
>
> **数据说明**：已提供 `exam_data.parquet` 文件，包含全A股日频行情数据，无需自行获取数据
>
> **提交方式**：将完成的 Notebook 文件命名为 `exam_QR_姓名.ipynb`，通过 https://www.ten-x-ai.com/interview/submit-assignment 提交

---

## 数据说明

已在当前文件夹下提供 `exam_data.parquet` 文件。

**数据概况**：
- 时间范围：2019-01-02 ~ 2023-12-29（1214个交易日）
- 股票数量：1000只（全A股抽样，已剔除北交所）
- 已剔除停牌日（volume=0）

**字段说明**：

| 字段名 | 说明 |
|--------|------|
| `trade_date` | 交易日期（int, YYYYMMDD格式） |
| `symbol` | 股票代码（如 000001.SZ） |
| `close` | 收盘价（未复权） |
| `volume` | 成交量（手） |
| `amount` | 成交额（万元） |
| `vwap` | 成交量加权平均价 |
| `adj_factor` | 复权因子 |
| `open` / `high` / `low` / `pre_close` | 开高低 / 昨收 |
| `turnover` | 换手率（总股本） |
| `turnover_float` | 换手率（流通股本） |
| `mv` / `mv_float` | 总市值 / 流通市值 |
| `ind_code` | 中信一级行业代码 |
| `new_ipo` | 是否次新股 |
| `limitupdown` | 全天是否为一字板涨停 |
| `limitupdown_at_close` | 收盘时是否涨跌停 |

---

## 题目背景

在量化选股研究中，价格和成交量是最基础的两类信息。传统做法通常将价格动量和成交量变化分别建模，再通过线性组合生成复合因子。本题要求你采用一种不同的思路：**将价量变化统一映射到极坐标系中**，利用极径刻画变化幅度、极角刻画变化方向，从而构建一个价量融合的反转因子。

---

## 第一题：因子构建与检验

### 1.1 数据预处理

- 加载提供的 `exam_data.parquet` 数据文件
- 进行必要的数据清洗，并说明你的处理逻辑和原因
- 展示处理后的数据基本统计信息

### 1.2 因子构建

构建基于极坐标体系的价量融合反转因子，整体思路为：对每只股票，度量其当前价量状态相对于 $N$ 日前的偏离，将偏离映射到极坐标系中，最终综合极径、极角和方向偏好生成因子值。

具体步骤如下：

**(a) 极径：马氏距离**

对于每只股票，以（收盘价，成交额）作为价量状态向量。在每个截面日，计算当前价量状态 $\mathbf{x}$ 与 $N$ 日前的价量状态 $\mathbf{y}$ 之间的马氏距离作为极径 $\rho$：

$$\rho = \sqrt{(\mathbf{x} - \mathbf{y})^\top \Sigma^{-1} (\mathbf{x} - \mathbf{y})}$$

其中 $\Sigma$ 为价量的协方差矩阵，使用过去 **60个交易日** 的数据进行估计。

**(b) 极角**

使用 $\text{arctan2}$ 函数计算极角 $\theta$，将价量变化的方向映射到 $[0, 2\pi)$ 区间。

**(c) 极角调整函数**

设计一个极角调整函数 $f(\theta)$，使得价量同向变化（即第一象限 $45°$ 方向）时因子权重最大，偏离该方向时权重衰减。

**(d) 偏好系数**

根据极角 $\theta$ 所在象限设置偏好系数 $\alpha$，反映不同价量变化组合对反转因子的贡献方向：

| 极角区间 | 价量含义 | $\alpha$ |
|----------|----------|:--------:|
| $[0, \frac{\pi}{2})$ | 价升量增 | $1$ |
| $[\frac{\pi}{2}, \pi)$ | 价跌量增 | $-0.5$ |
| $[\pi, \frac{3\pi}{2})$ | 价跌量缩 | $-1$ |
| $[\frac{3\pi}{2}, 2\pi)$ | 价升量缩 | $0.75$ |

**(e) 最终因子**

单周期因子计算公式：

$$\text{factor} = \alpha \cdot \rho \cdot f(\theta)$$

分别计算 **20日、60日、120日、240日** 四个回看周期（$N$）的因子，并将 **四个周期因子等权合成**，得到复合因子。

**注意**：第一题中，因子计算完成后**直接进入因子检验**，不需要对因子做去极值、标准化、中性化等后处理。如果你认为因子后处理能带来改善，可以作为第二题的优化方向进行探索。

### 1.3 因子检验

对上述构建的因子（包括单周期因子和复合因子）进行有效性检验，展示检验结果并给出你的分析。

---

## 第二题：开放性优化（选作加分题）

> **重要说明**：第二题为**选作题**，不做不扣分（第二题各项计 0 分），做了视完成质量给予额外加分。请优先确保第一题的完整性和质量。

在第一题的基础上，**选择2个优化方向**进行探索。你需要：

1. **优化规划**：在实验开始前，写清楚你的优化路径规划，包括选择了哪些优化方向、为什么选择这些方向、每个方向的预期假设、实验设计方案（参数范围、对比基准、评估指标等）

2. **实验执行**：对每个优化方向，完成代码实现和结果展示，与第一题的基准因子进行对比，展示关键的对比图表

3. **结论总结**：总结哪些优化有效、哪些无效及原因，给出你认为的最优因子方案，以及如果有更多时间还会尝试的方向

---

## 提交说明

请通过以下链接提交作业：https://www.ten-x-ai.com/interview/submit-assignment

1. **文件命名**：将 Notebook 命名为 `exam_QR_姓名.ipynb`（例如 `exam_QR_张三.ipynb`）
2. **提交要求**：
   - 使用邮箱或电话号码进行身份验证
   - 将 `.ipynb` 文件放入 ZIP 后上传（提交一个 ZIP，内含该 ipynb）
   - 如有特殊需求，请与 HR 联系

   > **关于运行输出（重要）**：
   >
   > 提交前请执行 **Kernel -> Restart & Run All**，确保所有代码单元格有 `execution_count` 编号和对应的输出结果。**评分将基于 Notebook 中实际可见的代码输出（而非 Markdown 文字声称的结论）来评定结果展示和分析归因类评分项。** 无运行输出的 Notebook 将在运行完整度维度被显著扣分。
3. 代码要求：
   - 结构清晰，有适当的函数封装
   - 关键步骤有注释说明
   - 能够直接运行复现结果
4. 文字分析要求：
   - 对每个步骤的结果给出简要分析
   - 对优化实验给出明确的结论
